import { en } from "@/i18n/en"
import { es } from "@/i18n/es"
import type { DashboardTranslations, LanguageCode } from "@/types/dashboard"

export const translations: Record<LanguageCode, DashboardTranslations> = {
  en,
  es,
}
