services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./models/pipelines:/app/models/pipelines
    environment:
      - PYTHONUNBUFFERED=1
